import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "ROYAL ETHNIC — Crafted for Royal Occasions",
  description: "Premium ethnic menswear. Sherwanis, Jodhpuri Suits, Kurta Sets and more. Where heritage meets haute couture.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
